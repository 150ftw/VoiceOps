{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm --prefix apps/web install && npm install --include=dev",
  "buildCommand": "npm --prefix apps/web run build",
  "outputDirectory": "apps/web/.next"
}
